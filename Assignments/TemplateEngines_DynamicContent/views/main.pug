extends layouts/main-layout.pug
block content 
    main
        form(action="/users", method="POST")
            .form-control 
                label(for="username") Username 
                input(type="text", name="username")#username
            button(type="submit") Add username